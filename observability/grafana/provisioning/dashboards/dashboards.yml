apiVersion: 1

providers:
  - name: TrackAlph
    orgId: 1
    folder: TrackAlph
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    options:
      path: /var/lib/grafana/dashboards
